spring.application.name=market

server.port=${PORT:8083}
app.jwt.secret=${JWT_SECRET}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

springdoc.api-docs.enabled=true
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.url=/v3/api-docs
springdoc.swagger-ui.disable-swagger-default-url=true

##Not implemented yet
#paystack.api.key=${PAYSTACK_API_KEY}
#paystack.api.secret=${PAYSTACK_SECRET_KEY}
#paystack.api.url=https://api.paystack.co
#paystack.callback.url=${APP_CALLBACK_URL}


# Email configuration
app.email.from=${APP_EMAIL_FROM:noreply@market.com}
app.frontend.url=${APP_FRONTEND_URL:http://localhost:4173}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.protocol=smtp

spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000


#For debug logging
logging.level.com.market=INFO