
  spring:
    application:
      name: market

    datasource:
      url: jdbc:postgresql://localhost:5432/market_db
      username: postgres
      password: ogwaa123
      driver-class-name: org.postgresql.Driver

      hikari:
        maximum-pool-size: 2

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      database-platform: org.hibernate.dialect.PostgreSQLDialect

    flyway:
      enabled: true
      baseline-on-migrate: true
      baseline-version: 0
      locations: classpath:db/migration

    mail:
      host: smtp.gmail.com
      port: 587
      username: ochogwuprince@gmail.com
      password: hmrxhxvcvksxapql

      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true

          debug: true

  server:
    port: 8083

  app:
    jwt:
      secret: mySecretKeyForJWTTokenGenerationAndValidationPurposeOnly123456789012
      expiration: 86400000

    email:
      from: ${APP_EMAIL_FROM:noreply@market.com}

    frontend:
      url: ${APP_FRONTEND_URL:http://localhost:4174}

  springdoc:
    api-docs:
      enabled: true
      path: /v3/api-docs

    swagger-ui:
      enabled: true
      path: /swagger-ui.html
      url: /v3/api-docs
      disable-swagger-default-url: true

  logging:
    level:
      org:
        flywaydb: DEBUG
        springframework:
          security: DEBUG
        hibernate:
          orm:
            jdbc:
              bind: TRACE

      com:
        market:
          config: DEBUG
          root: INFO